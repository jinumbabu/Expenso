name: sqlite3_flutter_libs
description: A dummy override package to delegate to sqlcipher_flutter_libs
version: 0.5.42

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqlcipher_flutter_libs: ^0.6.2
